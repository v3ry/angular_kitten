import { ShowCatDirective } from './show-cat.directive';

describe('ShowCatDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowCatDirective();
    expect(directive).toBeTruthy();
  });
});
